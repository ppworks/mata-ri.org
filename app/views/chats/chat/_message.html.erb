<div class="message <%= chat.style %>">
  <span style="color: <%= chat.color %>"><%= chat.user_name %></span>
  <span class="content"><%= t('.content_html', content: chat.content, color: chat.color) %></span>
  <time><%= chat.created_at.strftime(t('date.long')) %></time>
</div>
